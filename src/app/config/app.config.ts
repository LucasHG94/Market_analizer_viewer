// A template of the initial configuration of the application.

import {IAppConfig} from '../model/index';

export const appConfig: IAppConfig = {
  host: '0.0.0.0',
  port: 5000,
  password: 'per123'
};
